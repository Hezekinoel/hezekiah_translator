[project]
name = "hezekiah-translator"
version = "0.1.0"
description = " An English translation tool for the terminal on Fedora Linux. "
readme = "README.md"
requires-python = ">=3.12"
license = {file = "LICENSE"}

dependencies = ["pyperclip", "deep-translator",]
